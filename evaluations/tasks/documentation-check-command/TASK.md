# Align the documented verification command

## Goal

Make the verification command in `README.md` agree with the exact script
published by `package.json`.

## Constraints

Treat the manifest as authoritative. Change documentation only and do not add,
rename, or duplicate a package script.

## Completion

The README gives one command that invokes the existing `check` script exactly.
