# Apply three page refinements

## Goal

Update `index.html` so the page background is green, the visible heading ends
with an exclamation mark, and the end-of-document comment is removed.

## Constraints

Change `index.html` only. Preserve the existing foreground color, document
structure, language, title, and centered layout. Add no file, dependency,
script, or unrelated formatting change.

## Completion

All three requested refinements are present together and no other page content
or file has changed.
